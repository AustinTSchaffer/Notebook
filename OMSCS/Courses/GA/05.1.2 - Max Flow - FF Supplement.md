---
tags:
  - OMSCS
  - Algorithms
  - Alg-Graph
---
# 05.1 - Max Flow - Ford-Fulkerson Supplement

1. Create our flow network f with flow = 0 for every edge. Overall flow is 0.

![[Pasted image 20260309231220.png]]

2. Build the residual network for $G^f$ for current flow.

![[Pasted image 20260309231243.png]]

2. The st-path P is in green above. This could be found algorithmically by running DFS starting at s on the residual network, and using the found st-path. Note: There are many st-paths currently, this was just chosen at random.

3. The minimum capacity $c(P) = 2$ for this st-path.

4. Augment $f$ by $c(P)$ units along $P$. Overall flow is 2.

![[Pasted image 20260309231331.png]]

5. Build the residual network for $G^f$ for current flow.

![[Pasted image 20260309231347.png]]

6. The st-path $P$ is in green above.

7. $c(P) = 3$ for this st-path.

8. Augment f by $c(P)$ units along $P$. Overall flow is $5$.

![[Pasted image 20260309231414.png]]

6. Build the residual network for $G^f$ for current flow.

![[Pasted image 20260309231434.png]]

6. The st-path $P$ is in green above.

7. $c(P) = 1$ for this st-path.

8. Augment $f$ by $c(P)$ units along $P$. Overall flow is 6.

> Notice the direction of the st-path edges compared to the original graph or flow network. One is in the opposite direction! Therefore, we **subtract** the flow along that edge, since the flow is going in the negative (opposite) direction. Consider why this is.

![[Pasted image 20260309231534.png]]

2. Build the residual network for $G^f$ for current flow. (using double arrows for visibility)

![[Pasted image 20260309231555.png]]

3. The st-path $P$ is in green above.

4. $c(P) = 3$ for this st-path.

5. Augment $f$ by $c(P)$ units along $P$. Overall flow is 9.

![[Pasted image 20260309231617.png]]

3. Build the residual network for $G^f$ for current flow.

![[Pasted image 20260309231641.png]]

4. The st-path $P$ is in green above.

5. $c(P) = 3$ for this st-path.

6. Augment $f$ by $c(P)$ units along $P$. Overall flow is 12.

![[Pasted image 20260309231707.png]]

7. Build the residual network for $G^f$ for current flow.

![[Pasted image 20260309231719.png]]

7. No st-path found. Maximum flow achieved.